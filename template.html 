<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>WebScanPro Security Report</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; background-color: #f4f4f4; }
        .container { max-width: 900px; margin: auto; background: #fff; padding: 30px; border-radius: 8px; }
        h1 { text-align: center; color: #333; }
        .finding { border: 1px solid #ddd; margin-bottom: 20px; padding: 15px; border-radius: 5px; }
        .finding h2 { margin-top: 0; }
        .details code { background-color: #eee; padding: 2px 5px; border-radius: 3px; }
        .severity-High { color: #d9534f; font-weight: bold; }
        .severity-Medium { color: #f0ad4e; font-weight: bold; }
        .severity-Low { color: #5cb85c; font-weight: bold; }
    </style>
</head>
<body>
    <div class="container">
        <h1>WebScanPro: Security Report</h1>

        {% if findings %}
            {% for finding in findings %}
            <div class="finding">
                <h2>{{ finding.type }}</h2>
                <div class="details">
                    <p><strong>Affected Endpoint:</strong> <code>{{ finding.endpoint }}</code></p>
                    <p><strong>Severity:</strong> <span class="severity-{{ finding.severity }}">{{ finding.severity }}</span></p>
                    <p><strong>Suggested Mitigation:</strong> {{ finding.mitigation }}</p>
                </div>
            </div>
            {% endfor %}
        {% else %}
            <div class="finding">
                <h2>No Vulnerabilities Found</h2>
                <p>The scan completed successfully with no vulnerabilities detected.</p>
            </div>
        {% endif %}
    </div>
</body>
</html>
